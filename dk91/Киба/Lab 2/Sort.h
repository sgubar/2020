#ifndef _SORT_H
#define _SORT_H

#include <stdlib.h>
#include <string.h>
#include <stdio.h>

    void sort( char* str,int  size );
    void sort2 (char *a, int size);
    void sort1(char a[], int n);
    void sort3(char a[], int n);
#endif
