#include <stdio.h>
#include <stdlib.h>
#include "hidder.h"

int main()
{
    int a, b, c, d = 0;
    scanf("%d %d %d", &a, &b , &c);
    double qwe;
    qwe = ((a * b)/(b + step(c, 2)))* sum(b, d) ;
    printf(" qwe%f", qwe);
    return 0;
}
  
